{
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "scripts": {
    "start": "concurrently \"npm run start:server\" \"npm run start:client\"",
    "start:server": "node ./backend/server.js",
    "start:client": "cd frontend && npm run dev",
    "installall": "cd frontend && npm install && cd ../backend && npm install"
  }
}
